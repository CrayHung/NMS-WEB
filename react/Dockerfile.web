# Dockerfile.web
FROM nginx:alpine

# 拷貝自訂設定
COPY nginx.conf /etc/nginx/conf.d/default.conf

# 拷貝本機 build 產物（Vite 預設 dist/）
COPY dist/ /usr/share/nginx/html/

EXPOSE 80

# 可選：健康檢查
HEALTHCHECK CMD wget -qO- http://localhost/ || exit 1
